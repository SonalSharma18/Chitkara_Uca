#include<stdio.h>
#include<math.h>
void main()
{
	int n,m,r,s=0;
 	printf("enter number");
 	scanf("%d",&n);
 	m=n;
 	while(m!=0)
 	{
  		r=m%10;
  		s=s+pow(r,3);
  		m=m/10;
 	}
    if(s==n)
  	printf("number is armstrong\n");
  	else 
  	printf("number is not armstrong\n");
}
  
