#include<stdio.h>
void main()
{
  char a[100][100]={0},b[100]={0},c[100]={0},d[100]={0},pass[100]={0};
  int i,j,k,l1,l2,l3,l4,l5,t=0;
  printf("enter strings");
  for(i=0;i<4;i++)
  {
    gets(a[i]);
    l1=strlen(a[i]);
    for(k=0;k<3;k++)
     {
       pass[t]=a[i][k];
        t++;
     }
}
 /* gets(a);
  gets(b);
  gets(c);
  gets(d);
 l1=strlen(a);
 l2=strlen(b);
 l3=strlen(c);
 l4=strlen(d);
 l5=12;
for(i=0;i<l5;)
 {
  for(j=0;j<3;j++)
    {
     
      pass[i]=a[j];
      i++;
      t++;
   
}

for(j=0;j<3;j++)
    {
    
      {
      pass[i]=b[j];
      i++;
      t++;
   }
}
 for(j=0;j<3;j++)
    
      {
       pass[i]=c[j];
      i++;
      t++;
   }
  
 for(j=0;j<3;j++)
    
     {
     pass[i]=d[j];
      i++;
      t++;
   }
 }*/

 for(k=0;k<12;k++)
   {
     if((pass[k]>=65 && pass[k]<=90)|| (pass[k]>=97 && pass[k]<=122))
    printf("%c",pass[k]);
 }
for(
}
   
   
   
 
